.PHONY: test
test:
	clojure -Xtest
help:
	clojure -M -m director.core -h
simulate:
	clojure -M -m director.core -s
run:
	clojure -M -m director.core
run2:
	clojure -M -m director.core -p planner_prompts/roberts_rules_planner_prompt_2.txt
